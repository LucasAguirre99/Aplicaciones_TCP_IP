apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: influxdb-claim0
  name: influxdb-claim0
  namespace: tcp-ip
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
